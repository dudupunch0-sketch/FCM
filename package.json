{
  "name": "fcm-combo-ring",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-config": "node tools/sync_config.mjs",
    "pretest": "node tools/sync_config.mjs",
    "test": "node --test tests/*.test.mjs",
    "prestart": "node tools/sync_config.mjs",
    "start": "python3 -m http.server 8000 --directory dist"
  }
}
