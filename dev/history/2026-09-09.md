# 2026-09-09 — 캐릭터 표현을 정하다

> 커밋 5개 (`d5c3986` … `aa03028`) · PR #4~#5
> 재구성 근거: 커밋 기록과 `docs/design/21a`, `21b`, `docs/art`

## 한 줄

선수를 **어떻게 보여줄 것인가**를 정하고, 모바일 픽셀 방향으로 수렴한 날.

## 흐름

1. 재사용 가능한 캐릭터 비율과 연속 페이스 메시 (`d5c3986`)
2. 참조 아트 워크플로 문서화 (`b13b00d`)
3. 오리지널 스킨드 블록아웃과 재현 가능한 메시 프리뷰 (`5d51859`)
4. **모바일 콤보 계획 UI와 픽셀 아트 파이터** (`aa03028`)

## 방향 전환

3D 관절 모델에서 **모바일 픽셀**로 옮겼다.
`docs/design/21b_mobile_pixel_combat.md`가 그 결정을 담는다.

이 전환이 남긴 원칙 하나: **계획 UI는 예상치를 보여주되 그것이 예상임을 명시한다.**
예상 스태미너 표기가 여기서 나왔고, 이후 거리 모델(22번)의 예상 거리 표기도 같은 규칙을 따른다.

## 참조 자료 취급

외부 참조 아트는 `docs/art`에 워크플로로만 남기고,
게임에 들어가는 것은 전부 오리지널 블록아웃으로 다시 만들었다.
